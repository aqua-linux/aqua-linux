[package]
name = "aqua-renderer"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
aqua-installer = { path = "../aqua-installer" }
aqua-scene = { path = "../aqua-scene" }
aqua-shell = { path = "../aqua-shell" }
fontdue = { version = "=0.9.3", default-features = false }
png = "0.18.1"

[lib]
path = "src/lib.rs"
