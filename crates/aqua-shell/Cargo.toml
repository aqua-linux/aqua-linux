[package]
name = "aqua-shell"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
aqua-components = { path = "../aqua-components" }
aqua-scene = { path = "../aqua-scene" }
aqua-service-adapters = { path = "../aqua-service-adapters" }

[lib]
path = "src/lib.rs"
