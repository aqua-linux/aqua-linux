[package]
name = "aqua-components"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
aqua-scene = { path = "../aqua-scene" }

[lib]
path = "src/lib.rs"
