#!/bin/sh

set -u

if [ "$#" -ne 6 ] || [ "$1" != -f ] || [ "$2" != -n ] ||
   [ "$3" != -i ] || [ "$4" != wlan0 ] || [ "$5" != -s ] ||
   [ "$6" != /usr/bin/aqua-udhcpc-hook ]; then
    echo '[AQUA-WIFI] stage=udhcpc-client status=blocked reason=invalid-arguments' >&2
    exit 2
fi
if [ ! -x /sbin/udhcpc ]; then
    echo '[AQUA-WIFI] stage=udhcpc-client status=blocked reason=busybox-applet-missing' >&2
    exit 2
fi

exec /sbin/udhcpc -f -n -i wlan0 -s /usr/bin/aqua-udhcpc-hook
