# The opt-in Aqua audio profile routes the ALSA default through the per-user
# PipeWire graph. The default Aqua image does not install this file.
pcm.!default {
    type pipewire
    playback_node "-1"
    capture_node "-1"
}

ctl.!default {
    type pipewire
}
